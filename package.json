{
  "name": "vanilla-basic",
  "version": "1.0.0",
  "scripts": {
    "start": "sirv ./src public --cors --single --no-clear --port 8000",
    "build": "npm run build",
    "build:azure": "npm run build"
  },
  "engines": {
    "node": ">= 14.0.0"
  }

}
